{
    "name": "coachlink/api",
    "description": "CoachLink CI — API REST en PHP pur (sans framework). PHPUnit est la seule dépendance, et uniquement pour les tests.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "scripts": {
        "test": "phpunit --colors=always"
    },
    "config": {
        "sort-packages": true
    }
}
