# Archives générées (livrables, non versionnés)
coachlink.zip
*.zip

# Dépendances de test
node_modules/

# Backend PHP — fichiers générés / secrets (non versionnés)
coachlink/api/config/config.php
coachlink/api/database/*.sqlite
coachlink/api/uploads/*
!coachlink/api/uploads/.gitkeep

# Dépendances de dev (PHPUnit) & cache de tests
coachlink/api/vendor/
coachlink/api/.phpunit.cache/
coachlink/api/.phpunit.result.cache
