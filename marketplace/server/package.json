{
  "name": "marche-ci-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend Node.js (Express) — vérification d'identité vendeur + reconnaissance faciale pour Marché CI",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "build:css": "tailwindcss -i ./src/input.css -o ./public/tailwind.css --minify",
    "dev:css": "tailwindcss -i ./src/input.css -o ./public/tailwind.css --watch"
  },
  "dependencies": {
    "express": "^4.22.2",
    "tailwindcss": "^3.4.17"
  }
}
